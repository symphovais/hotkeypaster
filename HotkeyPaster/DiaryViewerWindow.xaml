<Window x:Class="TalkKeys.DiaryViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Diary Viewer"
        Width="900"
        Height="600"
        MinWidth="700"
        MinHeight="500"
        Background="{StaticResource BackgroundPrimary}"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar: Date List and Search -->
        <Border Grid.Column="0"
                Background="{StaticResource BackgroundSecondary}"
                Padding="20"
                BorderBrush="{StaticResource BorderDefault}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0"
                          Text="Diary Entries"
                          FontSize="20"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextPrimary}"
                          Margin="0,0,0,20"/>

                <!-- Search Box -->
                <Border Grid.Row="1"
                        Background="{StaticResource BackgroundPrimary}"
                        CornerRadius="8"
                        Margin="0,0,0,16"
                        Padding="12,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                  Text="🔍"
                                  FontSize="14"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"/>

                        <TextBox x:Name="SearchBox"
                                Grid.Column="1"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{StaticResource TextPrimary}"
                                CaretBrush="{StaticResource AccentPrimary}"
                                FontSize="14"
                                VerticalAlignment="Center"
                                TextChanged="SearchBox_TextChanged">
                            <TextBox.Resources>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Grid>
                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                VerticalAlignment="Center"/>
                                                    <TextBlock Text="Search entries..."
                                                             Foreground="{StaticResource TextMuted}"
                                                             IsHitTestVisible="False"
                                                             VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </Grid>
                </Border>

                <!-- Date List -->
                <ScrollViewer Grid.Row="2"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Disabled">
                    <ListBox x:Name="DateListBox"
                            Background="Transparent"
                            BorderThickness="0"
                            SelectionChanged="DateListBox_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Border"
                                                   Background="{TemplateBinding Background}"
                                                   CornerRadius="8"
                                                   Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundPrimary}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentPrimary}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                             Text="{Binding DisplayDate}"
                                             FontSize="14"
                                             FontWeight="Medium"
                                             Foreground="{StaticResource TextPrimary}"/>

                                    <TextBlock Grid.Row="1"
                                             Text="{Binding EntryCountText}"
                                             FontSize="12"
                                             Foreground="{StaticResource TextMuted}"
                                             Margin="0,2,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>

                <!-- Bottom Stats -->
                <Border Grid.Row="3"
                       Background="{StaticResource BackgroundPrimary}"
                       CornerRadius="8"
                       Padding="12"
                       Margin="0,16,0,0">
                    <StackPanel>
                        <TextBlock x:Name="TotalEntriesText"
                                 Text="0 total entries"
                                 FontSize="12"
                                 Foreground="{StaticResource TextMuted}"
                                 HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Right Panel: Entry Display -->
        <Grid Grid.Column="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="SelectedDateText"
                             Text="Select a date"
                             FontSize="24"
                             FontWeight="SemiBold"
                             Foreground="{StaticResource TextPrimary}"/>

                    <TextBlock x:Name="EntryCountText"
                             Text=""
                             FontSize="14"
                             Foreground="{StaticResource TextMuted}"
                             Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📂 Open Folder"
                           Style="{StaticResource SecondaryButton}"
                           Click="OpenFolderButton_Click"
                           Padding="16,10"
                           Margin="0,0,12,0"/>

                    <Button Content="📝 New Entry"
                           Style="{StaticResource PrimaryButton}"
                           Click="NewEntryButton_Click"
                           Padding="16,10"/>
                </StackPanel>
            </Grid>

            <!-- Entries Display -->
            <ScrollViewer Grid.Row="1"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="EntriesPanel">
                    <!-- Entries will be added dynamically -->

                    <!-- Empty State -->
                    <Border x:Name="EmptyState"
                           Background="{StaticResource BackgroundSecondary}"
                           CornerRadius="12"
                           Padding="40"
                           Visibility="Visible">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="📔"
                                     FontSize="48"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,16"/>
                            <TextBlock Text="No entries yet"
                                     FontSize="18"
                                     FontWeight="Medium"
                                     Foreground="{StaticResource TextPrimary}"
                                     HorizontalAlignment="Center"/>
                            <TextBlock Text="Press Ctrl+Shift+D to create your first diary entry"
                                     FontSize="14"
                                     Foreground="{StaticResource TextMuted}"
                                     HorizontalAlignment="Center"
                                     Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
